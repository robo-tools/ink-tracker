{
  "name": "chase-card-tracker-userscripts",
  "version": "1.0.2",
  "private": true,
  "type": "module",
  "description": "Local-first Tampermonkey trackers for Chase Ink rewards and World of Hyatt card elite-night spend.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "check": "npm run build && npm test",
    "verify-release": "node scripts/verify-release.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
