{
  "name": "chase-card-tracker-userscripts",
  "version": "1.2.2",
  "private": true,
  "type": "module",
  "description": "Local-first Tampermonkey trackers for Chase cards and Capital One Shopping rewards.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check-built": "node scripts/check-built.mjs",
    "test": "node --test",
    "check": "npm run build && npm run check-built && npm test",
    "verify-release": "node scripts/verify-release.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
